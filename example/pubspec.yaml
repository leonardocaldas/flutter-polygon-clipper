name: example
description: An example application using Flutter Polygon Clipper plugin.

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  polygon_clipper: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  polygon_clipper:
    path: ../

flutter:
  uses-material-design: true
